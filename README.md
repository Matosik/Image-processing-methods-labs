
# Фильтрация шумов


# Могут пригодиться функции:

- from skimage.util import random_noise
- from scipy.signal import convolve2d
- from scipy.ndimage import median_filter
- np.random.normal

# Ограничения: отсутствуют

- Шахматное поле генерируйте как хотите
- Можно самим, можно найти функцию

-------------------------------------------------------------

1) касательно пункта задания ПРОИЗВЕСТИ ПОВТОРНУЮ ФИЛЬТРАЦИЮ - можно НЕ делать.

Полезные советы:
2) Можно попробовать подобрать макс. и мин. шахматного поля таким образом, чтобы аддитивный шум не вылетал за диапазон 0..255 ( с р=0.997 конечно - по трем сигма ), когда вы возьмете его дисперсию равной дисперсии изображения ( эксперимент с отношением сигнал/шум=1).

Волшебные значения: 160 и 96.
! - нужно уметь сказать почему.
! - нужно уметь получить(вывести), как будто-бы изначально их не зная.

Полученное шахматное поле затем использовать во ВСЕХ экспериментах.

3) При работе с изображениями опять может быть удобно перенести 0 в 128 (генерировать все шумы со средним=128). Не забудьте обратно вычитать 128 при добавлении шума

4) При отчете должны быть представлены:

а) следующие изображения
1. исходное шах. поле
2. четыре шума - два белых (сигнал/шум = 1 и 10)
    - два импульсных (вероятности 0.1 и 0.3)
3. четыре зашумленных изображения
4. фильтрованные изображения ( 4 x 2 способа = 8 штук )

б) следующие цифры должны быть выведены на печать (в консоль)
1. дисперсии всех исходных изображений и шумов
2. восемь дисперсий ошибок фильтрации (дисперсий шума после фильтрации изображения)
3. восемь коэффициентов подавления шума



# Задания 

## Часть 1 Фильтрация аддитивного белого шума
1. Написать функцию для генерации Gray-scale изображение "шахматного поля" 8x8 клеток, каждая клетка 16x16пикселей. 

2. Написать функцию рассчета  дисперсии сгенерированного изображение

3. Написать функцию накладывающую аддитивный белый шум отношение сигнал/шум принять равным 10.

4. Написать функцию, которая фильтрует искаженное изображение медианным фильтром. В функции так же рассчитать квадрат среднего квадратичного отклонения фильтрации и коэффициент снижения шума

5. Написать функцию, которая фильтрует искаженное изображение линейным сглаживающим фильтром. В функции так же рассчитать квадрат среднего квадратичного отклонения фильтрации и коэффициент снижения шума

6. Повторить исследования приням отношение сигнал шум = 1.

## Часть 2. Фильтрация импульсного шума

1. Написать функцию накладывающую импульсный шум интенсивность p=0.1. Рассчитать среднеквадратичное отклонение

2. Написать функцию, которая фильтрует искаженное изображение медианным фильтром. В функции так же рассчитать квадрат среднего квадратичного отклонения фильтрации и коэффициент снижения шума

3. Написать функцию, которая фильтрует искаженное изображение линейным сглаживающим фильтром. В функции так же рассчитать квадрат среднего квадратичного отклонения фильтрации и коэффициент снижения шума

4. Повторить исследования, приняв интенсивность импульсного шума p=0.3 