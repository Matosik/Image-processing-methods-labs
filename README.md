
# Фильтрация шумов


# Могут пригодиться функции:

- from skimage.util import random_noise
- from scipy.signal import convolve2d
- from scipy.ndimage import median_filter
- np.random.normal

# Ограничения: отсутствуют

- Шахматное поле генерируйте как хотите
- Можно самим, можно найти функцию
- Собтвенно задание изложено в методичке

-------------------------------------------------------------

1) касательно пункта задания ПРОИЗВЕСТИ ПОВТОРНУЮ ФИЛЬТРАЦИЮ
- можно НЕ делать.

Полезные советы:
2) Можно попробовать подобрать макс. и мин. шахматного поля таким образом, чтобы аддитивный шум не вылетал за диапазон 0..255 ( с р=0.997 конечно - по трем сигма ), когда вы возьмете его дисперсию равной дисперсии изображения ( эксперимент с отношением сигнал/шум=1).

Волшебные значения: 160 и 96.
! - нужно уметь сказать почему.
! - нужно уметь получить(вывести), как будто-бы изначально их не зная.

Полученное шахматное поле затем использовать во ВСЕХ экспериментах.

3) При работе с изображениями опять может быть удобно перенести 0 в 128 (генерировать все шумы со средним=128). Не забудьте обратно вычитать 128 при добавлении шума

4) При отчете должны быть представлены:

а) следующие изображения
1. исходное шах. поле
2. четыре шума - два белых (сигнал/шум = 1 и 10)
    - два импульсных (вероятности 0.1 и 0.3)
3. четыре зашумленных изображения
4. фильтрованные изображения ( 4 x 2 способа = 8 штук )

б) следующие цифры должны быть выведены на печать (в консоль)
1. дисперсии всех исходных изображений и шумов
2. восемь дисперсий ошибок фильтрации (дисперсий шума после фильтрации изображения)
3. восемь коэф-тов подавления шума
4. Ваша фамилия